import { CrossSectionedItem } from '../../types/schedule';

interface CrossSectionedCardItemProps {
  scheduleItem: CrossSectionedItem;
}

export default function CrossSectionedCardItem(props: CrossSectionedCardItemProps) {
  return null;
}

import { UserInfo } from '../../types/store';

interface DetailProps {
  info: UserInfo;
  onBackPress: () => void;
}

export default function Details({ info, onBackPress }: DetailProps) {
  const {  }
}

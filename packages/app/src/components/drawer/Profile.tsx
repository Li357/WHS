import React from 'react';
import styled from 'styled-components/native';

import { DRAWER_INFO_MARGIN_TOP, DRAWER_INFO_MARGIN_BOTTOM } from '../../constants/style';

const ProfileContainer = styled.View`
  margin: ${DRAWER_INFO_MARGIN_TOP} ${DRAWER_INFO_MARGIN_BOTTOM};
`;

export default function Profile() {
  return (
    <ProfileContainer>

    </ProfileContainer>
  );
}
